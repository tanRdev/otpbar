# Allow a bounded adaptive menubar popover

The v2 popover may grow from 320 × 420 toward a 360 × 500 target when content or accessibility settings require it, while remaining bounded to the active screen's usable area and preserving menubar anchoring. A fixed canvas cannot reliably fit monitoring status, accessible controls, and recoverable errors; an unbounded window would stop feeling like a compact macOS menubar utility.
