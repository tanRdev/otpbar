# Require consent before Auto-copy

Auto-copy remains off until the user explicitly consents during onboarding. After consent, the user may configure a global policy and Provider-specific overrides; declining consent does not block manual copy or monitoring. Clipboard mutation is security-sensitive and surprising, so convenience does not justify an implicit default.
